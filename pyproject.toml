[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "offline-autonomous-node-coordination"
version = "0.1.0"
description = "Backend baseline for offline autonomous node coordination"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["offline_coordination"]
